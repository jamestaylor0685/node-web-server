<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8"/>
    <title>{{ page.title }}</title>
  </head>
  <body>
     {{> header}}
    <p>{{ screamIt page.welcomeMessage }}</p>
     {{> footer }}
  </body>
</html>